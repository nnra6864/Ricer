from Color import Color
import toml

class File:
    def __init__(self, name, path, form):
        self.name = name
        self.path = path
        self.format = form
        self.keyword = keyword


def load_config:
    
